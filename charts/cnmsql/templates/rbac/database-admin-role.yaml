{{- if .Values.rbac.helpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "cnmsql.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "cnmsql.resourceName" (dict "suffix" "database-admin-role" "context" $) }}
rules:
- apiGroups:
  - mysql.cnmsql.co
  resources:
  - databases
  verbs:
  - '*'
- apiGroups:
  - mysql.cnmsql.co
  resources:
  - databases/status
  verbs:
  - get
{{- end }}
